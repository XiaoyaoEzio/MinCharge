package com.min.charge.buffer.data;

public enum ThreadOffline {
	/*Instance;
	
	private static final Logger logger = Logger.getLogger(ThreadOffline.class);
	public boolean running = false;
	
	Thread thread = new Thread(new Runnable() {
		
		@Override
		public void run() {
			while (running) {
				
				new DataChargeInfo().refresh();
				try {
					Thread.sleep(60*1000);
				} catch (InterruptedException e) {
					logger.error(e.getMessage(),e);
				}
			}
		}
	});

	public void start(){
		thread.setName("thread_offline");
		running = true;
		logger.info("Start Thread_Offine");
		thread.start();
	}
	
	public void stop(){
		running = false;
		logger.info("Stop Thread_Offine");
	}*/
	
	
}
